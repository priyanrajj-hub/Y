<!--
  Add these lines inside the <manifest> tag of:
  android/app/src/main/AndroidManifest.xml

  (This file is generated by `flutter create`, so it isn't included in this
  zip — merge these permissions into it after you generate the project.)
-->

<!-- Required on Android 12+ (API 31+) -->
<uses-permission android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation" />
<uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

<!-- Required on Android 11 and below (API 30-) for BLE scanning -->
<uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

<!-- Declares the app uses BLE hardware; set required="false" if you want
     the app installable on devices without BLE, though it won't be very
     useful without it for this test -->
<uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
